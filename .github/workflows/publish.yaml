name: Publish

on:
  push:
    tags:
      - v*
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag to publish'
        required: false

permissions:
  contents: read
  packages: write

env:
  OCI_URL: ghcr.io/openkcm

jobs:
  released:
    uses: openkcm/build/.github/workflows/publish.svc.yaml@6965ab131696506a65dbd5b257dc73812b7f86b0
    with:
      image_signing_enabled: true  # Passed as the boolean 'true'
      ocm_signing_enabled: true   # Passed as the boolean 'false'
    secrets: inherit
