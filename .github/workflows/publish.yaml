name: Publish

on:
  push:
    tags:
      - v*
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag to publish'
        required: false

permissions:
  contents: read
  packages: write

env:
  OCI_URL: ghcr.io/openkcm

jobs:
  released:
    uses: openkcm/build/.github/workflows/publish.svc.yaml@e6060ee27beba5ba00866c5a37a89a4e21074122
    with:
      image_signing_enabled: true  # Passed as the boolean 'true'
      ocm_signing_enabled: true   # Passed as the boolean 'true'
    secrets: inherit
