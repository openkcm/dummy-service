name: "CodeQL::Analyze"

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"
  schedule:
    - cron: '16 11 * * 5'

permissions:
  contents: read

jobs:
  analyze:
    uses: openkcm/build/.github/workflows/codeql.yaml@main
    secrets: inherit
