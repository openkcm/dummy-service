name: New Release (Manual Trigger)

on:
  workflow_dispatch:

permissions:
  pull-requests: write

jobs:
  release:
    uses: openkcm/build/.github/workflows/release-manual.yaml@main
    secrets: inherit

